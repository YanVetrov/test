// Add your native dependencies here:

android {
  defaultConfig {  
    generatedDensities = []
    applicationId = "ai.azara.accepter"
  }
  aaptOptions {  
    additionalParameters "--no-version-vectors"  
  }  
} 
